extends Node

# tänne se pelin päälogiikka. tää laitetaan projektitiedostoon pääscriptiks
# niin kaikista scripteistä voi helposti tarttua tähä

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	print("peli ois päällä ja tää olis pää_scripti")
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
